1. What is the difference between var, let, and const?
   var is old and less safe
   let is used when value may changes
   const is used when value should not change
2. What is the spread operator (...)?
   spead operator is used to copy or expand values from arrays or objects.
3. What is the difference between map(), filter(), and forEach()?
   map() used for transform data
   filter() is used for selecting spacific items
   forEach() is used for loop through items
4. What is an arrow function?
   the shorter way to write functions
5. What are template literals?
   template literals allow to write strings with varialbles inside using backticks
